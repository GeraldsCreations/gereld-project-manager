rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read access (view-only dashboard)
    match /{document=**} {
      allow read: if true;
      allow write: if false; // Only backend can write via service account
    }
  }
}
